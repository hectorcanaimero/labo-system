---
type: spike
id: S8
slug: multi-tenant
project_id: labo-system
timebox: 1d (solo diseño)
depends_on: []
blocks: []
generated_by: orch-spec
generated_at: 2026-08-23
title: "S8 · Diseño multi-tenant para Fase 2 (sólo diseño)"
---

# S8 · Diseño multi-tenant (Fase 2)

## Objetivo

Documentar CÓMO se agregaría multi-tenant en Fase 2 sin refactor global. Es un spike **exclusivamente de diseño** — no bloquea nada de v1 ni implementa nada. Su valor es dejar preparado el terreno para no lamentar decisiones tempranas.

## Contexto

- PRD §4.2: multi-tenant está fuera de scope v1 (mono-tenant confirmado).
- PRD §10 (S8): diseñar `laboratorio_id` opcional desde v1, RLS lógica en queries.
- ARCH no propone `laboratorio_id` en v1 — schema es puro mono-tenant.
- Sin este análisis, agregar multi-tenant en Fase 2 requeriría migración destructiva.

## Alcance

Sí hace:
- Comparar dos aproximaciones:
  1. **Schema-level**: agregar `laboratorio_id: v.optional(v.id("laboratorios"))` en TODAS las tablas de dominio (pacientes, examenes_titulos, examenes, paquetes, resultados, presupuestos). En v1 queda `undefined`; en Fase 2 se popula con migración one-shot.
  2. **Application-level**: helper `withTenant(ctx, query)` que en v1 es no-op; en Fase 2 filtra por `laboratorio_id` derivado de `ctx.auth`.
- Analizar impacto en índices Convex: ¿los índices actuales sirven o hay que agregar `by_laboratorio_*`?
- Analizar impacto en Convex Auth / RBAC: la tabla `usuarios` necesita `laboratorio_id` en Fase 2.
- Analizar impacto en File Storage (assets por laboratorio).
- Analizar impacto en scraper BCV (¿cron por laboratorio o global?).
- Recomendación firmada con qué agregar en v1 (si algo) sin overhead.

No hace:
- Implementar nada.
- Cambiar schema v1.

## Criterios de aceptación

- [ ] Documento comparativo con las 2 aproximaciones y trade-offs.
- [ ] Recomendación explícita: "en v1 no tocar nada" vs "en v1 agregar `laboratorio_id` opcional".
- [ ] Lista de campos/índices que cambiarían en Fase 2 (checklist para futuro spike Fase 2).
- [ ] Análisis de impacto en `packages/convex/helpers/auth.ts` (`requireRole` + `requireTenant` hipotético).
- [ ] Nota clara: "esto NO se implementa en v1, es sólo diseño defensivo".

## Learning outputs

- Roadmap concreto para Fase 2 multi-tenant.
- Decisión informada sobre si vale agregar `laboratorio_id` en v1 (default: NO agregar — mono-tenant confirmado).

## Blocked features

Ninguna. Este spike es informativo.

## Notas técnicas

- Convex no tiene RLS a nivel DB (como Postgres). RLS es aplicativa (helper + convención).
- Índices Convex se pueden agregar sin migración (Convex re-indexa en background).
- Riesgo real de posponer: si Fase 2 llega, hay que migrar `_id` implícitamente asumidos como globales.
