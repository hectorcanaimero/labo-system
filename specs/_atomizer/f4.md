---
type: spec
phase: 4
project_id: labo-system
generated_by: orch-tasks-transformer
generated_at: 2026-08-23
title: "F4 — Hardening + Cutover"
---

# F4 — Hardening + Cutover

## F4.1 — Package: hardening

### F4.1.T1 — Empty states + loading skeletons en todas las listas

**Objetivo**

Cada lista y ficha tiene empty state ilustrado y loading skeleton (no spinner genérico).

**Alcance**

Sí hace:
- Empty states en: pacientes, resultados, presupuestos, paquetes, exámenes, dashboard, config.
- Skeletons para: cards KPI, tablas, formularios.
- Componente reusable `packages/ui/feedback/EmptyState.tsx` y `packages/ui/feedback/Skeleton.tsx`.
- Copy con voz del producto (español VE, calidez).

**Criterios de aceptación**

- [ ] Cada lista muestra empty state en dataset vacío.
- [ ] Loading skeleton visible en carga inicial.
- [ ] Sin spinners genéricos.

- **Modelo**: opencode/claude-sonnet-4-6
- **Estimación**: 6h
- **Razón**: Task de implementación estándar — Sonnet balancea costo/calidad.
- **Dependencies**:
- **Files**:
  - `packages/ui/feedback/EmptyState.tsx`
  - `packages/ui/feedback/Skeleton.tsx`
  - `Integraciones en cada página de F1/F2/F3.`

### F4.1.T2 — Error boundaries + toasts de error consistentes

**Objetivo**

Error boundary por sección + sistema de toasts unificado (reusar `sonner` o shadcn toast).

**Alcance**

Sí hace:
- `apps/web/app/(app)/error.tsx` boundary root.
- Per-section: `apps/web/app/(app)/[modulo]/error.tsx`.
- Helper `packages/ui/feedback/toast.ts` con `notifyError`, `notifySuccess`, `notifyInfo`.
- Helper `packages/lib/error-messages.ts`: mapping errores conocidos → texto humano (códigos de dominio `CEDULA_DUPLICADA`, etc. y errores PG 23505/23503 → códigos).
- Refactor fetchers/mutaciones: capturar error → toast con mensaje humano-legible.
- Todos los errores backend (repos + Route Handlers) mapeados.

**Criterios de aceptación**

- [ ] Excepción no capturada → error boundary muestra "Algo salió mal" con reset.
- [ ] Todos los errores conocidos tienen mensaje humano en toast.
- [ ] Errores 401 en API redirigen a login.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 5h
- **Razón**: Task de implementación estándar — deepseek cubre boundaries + mapping.
- **Dependencies**:
- **Files**:
  - `apps/web/app/(app)/error.tsx`
  - `apps/web/app/(app)/*/error.tsx`
  - `packages/ui/feedback/toast.ts`
  - `packages/lib/error-messages.ts`

### F4.1.T3 — Validaciones edge (fechas, límites, XOR checks)

**Objetivo**

Cubrir edge cases identificados en QA: fechas absurdas, cédulas rebordes, montos negativos, descuento > 100%, tasa 0.

**Alcance**

Sí hace:
- Refuerzo Zod schemas con `.refine` para:
  - Fecha muestra ≤ hoy.
  - Fecha resultado ≥ fecha muestra si ambas.
  - Descuento 0-100.
  - Ganancia ≥ 0.
  - Tasa > 0.
  - Cédula sin caracteres raros.
- Backend defensive check duplicado (repos validan además de UI; CHECKs de DDL como última red).
- Tests unitarios + de integración cubriendo cada edge (integración contra Postgres real o contenedor efímero).

**Criterios de aceptación**

- [ ] Fecha muestra futura rechaza.
- [ ] Descuento 150% rechaza en form y backend.
- [ ] Presupuesto con `paciente_id` + `nombre_libre` rechaza en backend (test integración — CHECK de DDL dispara).
- [ ] Test coverage de schemas > 90%.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 5h
- **Razón**: Task de implementación estándar — deepseek cubre schemas + tests integración.
- **Dependencies**: F2.1.T1, F2.3.T1, F2.4.T1
- **Files**:
  - `packages/lib/schemas/*.ts`
  - `packages/db/repos/*.ts`

### F4.1.T4 — Warm-up ping PDF + health check

**Objetivo**

Mantener el runtime Node del Route Handler PDF warm y verificar salud del servicio.

**Contexto**

ARCH §9 riesgo cold-start. ADR-11: self-hosted en VPS — el cold start es menos crítico que serverless, pero el warm-up de `@react-pdf` sigue ayudando al primer render.

**Alcance**

Sí hace:
- Endpoint `apps/web/app/api/pdf/health/route.ts` (Node runtime) que:
  - Retorna 200 OK + status de conexión a Postgres.
  - Opcional: render dummy mini (1KB PDF) para calentar `@react-pdf`.
- Entrada de crontab cada 5min en horario laboral llamando al endpoint (mismo patrón CRON_SECRET).
- Métrica `pdf_health_ok` en logs.

**Criterios de aceptación**

- [ ] `/api/pdf/health` responde 200.
- [ ] Cron activo en crontab del VPS.
- [ ] Métrica visible en logs.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 2h
- **Razón**: Task pequeña — deepseek alcanza.
- **Dependencies**: F2.3.T5
- **Files**:
  - `apps/web/app/api/pdf/health/route.ts`
  - `docs/deploy/insforge-vps.md`

### F4.1.T5 — Tests E2E Playwright de flujos críticos

**Objetivo**

Suite Playwright cubriendo flujos que si fallan bloquean operación: login, crear paciente, crear resultado + PDF, crear presupuesto + convertir, exportar CSV.

**Alcance**

Sí hace:
- `apps/web/e2e/` con Playwright config.
- Test `auth.spec.ts` (login OK, login fail).
- Test `paciente.spec.ts` (crear + buscar).
- Test `resultado.spec.ts` (crear + descargar PDF).
- Test `presupuesto.spec.ts` (crear + aprobar + convertir a resultado).
- Test `export.spec.ts` (exportar presupuestos).
- Fixtures seed data.
- CI job E2E (separado, con headless Chrome).

**Criterios de aceptación**

- [ ] 5 tests pasan en local + CI.
- [ ] Tiempo total < 5min.
- [ ] Screenshot on failure.

- **Modelo**: opencode/claude-sonnet-4-6
- **Estimación**: 10h
- **Razón**: Task de implementación estándar — Sonnet balancea costo/calidad.
- **Dependencies**:
- **Files**:
  - `apps/web/e2e/*.spec.ts`
  - `apps/web/playwright.config.ts`
  - `.github/workflows/e2e.yml`

### F4.1.T6 — Audit log dashboard (Admin only)

**Objetivo**

Página Admin `/audit` para ver últimos N eventos del `audit_log`.

**Contexto**

ARCH §7.3.

**Alcance**

Sí hace:
- `packages/db/repos/audit.ts` con `list({ page?, limit?, filters })`.
- Endpoint `GET /api/audit` (Admin only).
- Página `/audit` (Admin only via middleware).
- Tabla con filtros por usuario, acción, entity, fecha.
- Sólo visualización (no edit).

**Criterios de aceptación**

- [ ] Admin ve logs; Operador redirigido.
- [ ] Filtros funcionan.
- [ ] Paginación.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 4h
- **Razón**: Task de implementación estándar — deepseek cubre repo + página.
- **Dependencies**: F0.2.T8, F0.2.T5
- **Files**:
  - `packages/db/repos/audit.ts`
  - `apps/web/app/api/audit/route.ts`
  - `apps/web/app/(app)/audit/page.tsx`
  - `apps/web/middleware.ts`

## F4.2 — Package: cutover

### F4.2.T1 — Dry-run completo migración contra copia MySQL prod

**Objetivo**

Ejecutar `migrate-wp --dry-run --verify` contra dump/copia de MySQL prod para reportar lo que va a migrar sin escribir.

**Contexto**

Depende de F1.migracion.T08 (`--verify` implementado). Reunión con Admin post-run para revisar reporte.

**Alcance**

Sí hace:
- Obtener dump MySQL prod (o snapshot readonly con timestamp T0).
- Correr `pnpm --filter migrate-wp run migrate -- --dry-run --verify` contra el dump.
- Generar reporte JSON + tabla humana con: counts esperados, conflictos detectados, warnings.
- Sesión con Admin (Rosa) para revisar y aprobar/ajustar `resolutions/pacientes.json`.
- Timeline documentado del run (cuánto tarda).

No hace:
- Escritura real (dry-run).

**Criterios de aceptación**

- [ ] Reporte con counts exactos (204+ pacientes, 250+ exámenes, etc.).
- [ ] Cero conflictos no resueltos.
- [ ] Admin firma la aprobación por escrito (comentario en tracker).
- [ ] Timeline < 60min conocido.

- **Modelo**: opencode/claude-sonnet-4-6
- **Estimación**: 6h
- **Razón**: Task de implementación estándar — Sonnet balancea costo/calidad.
- **Dependencies**: F1.3.T8, F4.1.T3
- **Files**:
  - `docs/migration/dry-run-<fecha>.md`
  - `scripts/migrate-wp/resolutions/pacientes.json`

### F4.2.T2 — Ventana de cutover: WP read-only + migración real

**Objetivo**

Ejecutar la migración real en ventana acordada, con plugin WP puesto en read-only durante la ejecución.

**Contexto**

PRD §7 (ventana coordinada), §8 rollout, ADR-11 (destino = Postgres de InsForge).

**Alcance**

Sí hace:
- Comunicación previa al equipo del laboratorio (email + WhatsApp Business).
- Pasos:
  1. Backup MySQL prod (dump completo con timestamp T0) + snapshot `pg_dump` del destino pre-migración.
  2. Poner plugin WP en modo mantenimiento / read-only (banner "sistema en actualización, no ingresar datos nuevos").
  3. Ejecutar `pnpm --filter migrate-wp run migrate -- --confirm` contra Postgres prod (`DATABASE_URL` del InsForge del VPS).
  4. Ejecutar `--verify` post-migración.
  5. Si verify OK → smoke tests (T03).
  6. Si smoke OK → go-live LabSystem (apps/web servida en el VPS junto a InsForge), quitar WP mantenimiento pero dejar en read-only.
- Runbook `docs/migration/CUTOVER-RUNBOOK.md` con cada paso, tiempo esperado, criterio de abort.

No hace:
- Fixes en vivo (aborta si falla; rollback = restaurar snapshot pg_dump + reactivar WP).

**Criterios de aceptación**

- [ ] Backup MySQL creado antes de tocar nada.
- [ ] Migración real termina sin errores.
- [ ] `--verify` retorna 100% match.
- [ ] LabSystem operativo post-migración.
- [ ] WP en read-only (banner o plugin de "solo lectura").
- [ ] Comunicación enviada al equipo.

**Notas técnicas**

Ejecutar en horario fuera de operativa (viernes noche o sábado madrugada). El deploy de apps/web en el VPS debe estar probado antes (build + servicio systemd/docker).

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 8h
- **Razón**: Task operativo crítico — deepseek con runbook detallado.
- **Dependencies**: F4.2.T1
- **Files**:
  - `docs/migration/CUTOVER-RUNBOOK.md`

### F4.2.T3 — Smoke tests post-cutover (checklist manual + Playwright)

**Objetivo**

Checklist manual + subset Playwright que valida los 10 flujos críticos con datos reales migrados.

**Alcance**

Sí hace:
- Checklist en `docs/migration/SMOKE-TESTS.md`:
  1. Login Admin y Operador.
  2. Dashboard carga con KPIs reales.
  3. Buscar 3 pacientes conocidos (por cédula, nombre).
  4. Abrir ficha de paciente con historial migrado.
  5. Crear resultado nuevo (paciente + exámenes).
  6. Descargar PDF de resultado migrado (verificar snapshots).
  7. Crear presupuesto (nombre libre + real).
  8. Convertir presupuesto a resultado.
  9. Exportar CSV de presupuestos.
  10. Verificar tasa BCV automática funciona (o manual override).
- Ejecutar suite Playwright de F4.hardening.T05 contra prod (con user de test).
- Reporte de smoke firmado por Admin.

**Criterios de aceptación**

- [ ] 10/10 items checklist manual pasan.
- [ ] Playwright E2E pasa contra prod.
- [ ] Admin firma go-live.

- **Modelo**: opencode/claude-sonnet-4-6
- **Estimación**: 4h
- **Razón**: Task de implementación estándar — Sonnet balancea costo/calidad.
- **Dependencies**: F4.2.T2, F4.1.T5
- **Files**:
  - `docs/migration/SMOKE-TESTS.md`

### F4.2.T4 — Plan de rollback + procedimiento post-cutover

**Objetivo**

Documentar y probar el procedimiento de rollback (WP read-only backup + procedimiento de re-import).

**Contexto**

PRD §8 rollback, ADR-11 (backups = pg_dump estándar).

**Alcance**

Sí hace:
- `docs/migration/POST-CUTOVER-PLAYBOOK.md` con:
  - Cómo funciona WP en modo read-only 30d como backup.
  - Procedimiento de emergencia: si LabSystem tiene bug bloqueante → cómo reactivar WP para operar.
  - Procedimiento de re-import de datos post-cutover si hay que rollback (export CSV desde Postgres → reimport a WP o restauración).
  - `pg_dump` programado diario (cron VPS) con retención 7d + export XLSX mensual.
- Ensayar el procedimiento de rollback en preview.

**Criterios de aceptación**

- [ ] Playbook completo y revisado.
- [ ] Ensayo de rollback documentado con evidencia.
- [ ] `pg_dump` periódico configurado y verificado (restore de prueba).

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 4h
- **Razón**: Task documental operativo — deepseek alcanza.
- **Dependencies**: F1.3.T8
- **Files**:
  - `docs/migration/POST-CUTOVER-PLAYBOOK.md`

### F4.2.T5 — Monitoreo uptime + primer mes post-cutover

**Objetivo**

Configurar monitoreo, alertas y checkpoint del primer mes.

**Contexto**

PRD §9 success (uptime 99.5%, cero P0/P1 15d post-cutover). ADR-11: stack self-hosted — monitoreo externo + logs del VPS.

**Alcance**

Sí hace:
- Monitoreo uptime externo (UptimeRobot o equivalente) contra `/api/pdf/health` y el health endpoint de InsForge; alerta si < 99.5%.
- Alerta email BCV stale (F3.bcv.T04 ya lo cubre).
- Métricas: logs del contenedor apps/web (docker logs / pm2), PDF render > 5s p95 desde logs de requests.
- Dashboard interno simple: página Admin `/audit` + logs; InsForge dashboard/diagnose para el backend.
- Ceremonia checkpoint día 15 y día 30 post-cutover con Admin.
- Documentar tickets/incidentes encontrados.

**Criterios de aceptación**

- [ ] Alertas activas (uptime + BCV stale).
- [ ] Checkpoints día 15 y 30 realizados.
- [ ] Reporte "primer mes" con métricas vs targets del PRD §9.
- [ ] Cero P0/P1 abiertos al día 15 (success criteria).

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 4h
- **Razón**: Task de config/operación — deepseek alcanza.
- **Dependencies**: F4.2.T3
- **Files**:
  - `docs/deploy/monitoreo.md`
  - `docs/migration/POST-CUTOVER-METRICS.md`
