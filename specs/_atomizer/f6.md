---
type: spec
phase: 6
project_id: labo-system
generated_by: orch-spec
generated_at: 2026-08-25
title: "F6 — UX Refinements, Auth Polish, Patient Clinical Rules, Hierarchy, Packages & Budget Pipeline"
---

# F6 — UX Refinements, Auth Polish, Patient Clinical Rules, Hierarchy, Packages & Budget Pipeline

## F6.1 — Package: auth-pacientes

### F6.1.T1 — Mover /login a la raíz / en Next.js App Router

**Objetivo**

Mover la pantalla de autenticación a la ruta raíz `/` de Next.js App Router (`apps/web/app/(auth)/page.tsx`), eliminar la ruta duplicada `/login` y configurar redirect 308 permanente en `next.config.mjs`.

**Alcance**

Sí hace:
- Mover `apps/web/app/(auth)/login/page.tsx` a `apps/web/app/(auth)/page.tsx`.
- Eliminar o dejar stub de redirect para `apps/web/app/(auth)/login/page.tsx`.
- Configurar regla de redirects permanentes HTTP 308 en `apps/web/next.config.mjs` mapeando `/login` -> `/`.

**Criterios de aceptación**

- [ ] `GET /` renderiza directamente la interfaz de login cuando no existe sesión activa.
- [ ] `GET /login` responde con redirección permanente HTTP 308 hacia `/`.
- [ ] La build y el árbol de rutas de Next.js reconocen `/(auth)/page.tsx` como el entrypoint principal.

- **Modelo**: opencode/gpt-5.6-codex
- **Estimación**: 1.0h
- **Razón**: Refactor arquitectural de routing en Next.js App Router y configuración de redirects 308 con GPT-5.6 Codex.
- **Dependencies**: F5.1.T4
- **Files**:
  - `apps/web/app/(auth)/page.tsx`
  - `apps/web/app/(auth)/login/page.tsx`
  - `apps/web/next.config.mjs`

### F6.1.T2 — Actualizar middleware.ts y Auth Guards en Server Pages

**Objetivo**

Actualizar la lógica de `middleware.ts` y los guards de autenticación en Server Components (`(app)/layout.tsx`) para usar `/` como ruta pública de login y redirigir usuarios autenticados a `/dashboard`.

**Alcance**

Sí hace:
- Actualizar `apps/web/middleware.ts` para que la ruta pública base de autenticación sea `/` y redirigir accesos no autenticados a `/`.
- Si un usuario ya autenticado con cookie/sesión válida accede a `/`, redirigir automáticamente a `/dashboard`.
- Actualizar server guards en `apps/web/app/(app)/layout.tsx` para redirigir a `/` ante sesiones inválidas o ausentes.

**Criterios de aceptación**

- [ ] Intento de acceso no autenticado a cualquier subruta protegida `/(app)/*` redirige de inmediato a `/`.
- [ ] Usuario con sesión activa navegando a `/` es redirigido de inmediato a `/dashboard`.
- [ ] No se producen loops de redirección infinita entre `/` y `/dashboard`.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 1.0h
- **Razón**: Lógica de middleware perimetral y guards de Server Components con DeepSeek V4 Pro.
- **Dependencies**: F6.1.T1
- **Files**:
  - `apps/web/middleware.ts`
  - `apps/web/app/(app)/layout.tsx`

### F6.1.T3 — Actualizar enlaces cliente y tests Playwright hacia /

**Objetivo**

Actualizar todas las referencias y enlaces cliente en formularios de recuperación, mensajes de error 401 y adaptar los tests de navegación inicial en Playwright hacia `/`.

**Alcance**

Sí hace:
- Actualizar constantes y utilidades en `packages/lib/error-messages.ts` cambiando `/login` por `/`.
- Actualizar links de regreso en `apps/web/app/(auth)/forgot-password/ForgotForm.tsx` y `apps/web/app/(auth)/reset-password/ResetForm.tsx` apuntando a `/`.
- Actualizar helpers y specs de Playwright (`apps/web/e2e/helpers.ts`, `apps/web/e2e/auth.spec.ts`) para ingresar a `/` en lugar de `/login`.

**Criterios de aceptación**

- [ ] Los enlaces "¿Ya tienes cuenta? Iniciar sesión" en formularios auth navegan a `/`.
- [ ] `packages/lib/error-messages.ts` no contiene referencias residuales a `/login`.
- [ ] Tests E2E de login en `apps/web/e2e/auth.spec.ts` inician en `/` y pasan en verde.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 1.0h
- **Razón**: Adaptación de links cliente en formularios auth y suite E2E Playwright con DeepSeek V4 Pro.
- **Dependencies**: F6.1.T1, F6.1.T2
- **Files**:
  - `packages/lib/error-messages.ts`
  - `apps/web/app/(auth)/forgot-password/ForgotForm.tsx`
  - `apps/web/e2e/auth.spec.ts`

### F6.1.T4 — Hardening de Password Recovery (endpoint /api/auth/reset y mock E2E)

**Objetivo**

Endurecer el endpoint server-side de recuperación de contraseña `/api/auth/reset` con InsForge Auth, actualizar utilidades en `@labo/lib/server/auth.ts` y extender el mock server local (`mock-insforge.cjs`) para soportar pruebas E2E determinísticas.

**Alcance**

Sí hace:
- Endurecer `apps/web/app/api/auth/reset/route.ts` validando email, emitiendo tokens seguros y manejando expiración.
- Actualizar helpers de auth en `packages/lib/server/auth.ts` para solicitud y confirmación de reset.
- Extender el mock server `apps/web/e2e/server/mock-insforge.cjs` con endpoints `/auth/v1/recover` y `/auth/v1/verify` para testing local sin internet.

**Criterios de aceptación**

- [ ] `POST /api/auth/reset` valida payload con Zod y devuelve respuestas estructuradas sin filtrar datos sensibles.
- [ ] `mock-insforge.cjs` procesa flujos de recuperación y verificación de tokens en suite de testing.
- [ ] Suite de pruebas E2E de recovery ejecuta de forma determinista y pasa en verde.

- **Modelo**: opencode/gpt-5.6-codex
- **Estimación**: 1.5h
- **Razón**: Hardening de endpoints de autenticación, servidor mock local y contratos con GPT-5.6 Codex.
- **Dependencies**: F6.1.T1
- **Files**:
  - `apps/web/app/api/auth/reset/route.ts`
  - `packages/lib/server/auth.ts`
  - `apps/web/e2e/server/mock-insforge.cjs`

### F6.1.T5 — Pacientes DDL & Zod: migración sexo NOT NULL y validaciones

**Objetivo**

Hacer obligatorio el campo `sexo` a nivel de base de datos PostgreSQL (`NOT NULL`), actualizar `schema.sql` y definir validaciones Zod estrictas con código de error `SEXO_REQUERIDO` junto con sus tests unitarios.

**Alcance**

Sí hace:
- Crear migración SQL `packages/db/migrations/0003_paciente_sexo_not_null.sql` actualizando filas nulas legadas y ejecutando `ALTER TABLE pacientes ALTER COLUMN sexo SET NOT NULL;`.
- Actualizar `packages/db/schema.sql` con el constraint `NOT NULL` en `pacientes.sexo`.
- Actualizar `packages/lib/schemas/paciente.ts` requiriendo `sexo: z.enum(["M", "F"], { errorMap: () => ({ message: "SEXO_REQUERIDO" }) })`.
- Crear tests unitarios en `packages/lib/schemas/paciente.test.ts`.

**Criterios de aceptación**

- [ ] Migración `0003_paciente_sexo_not_null.sql` aplica limpia sobre PostgreSQL.
- [ ] Schema Zod rechaza payloads de paciente sin `sexo` emitiendo mensaje `SEXO_REQUERIDO`.
- [ ] Tests unitarios `pnpm --filter @labo/lib test` pasan al 100% en verde.

- **Modelo**: opencode/gpt-5.6-codex
- **Estimación**: 1.0h
- **Razón**: Migración SQL DDL y esquemas Zod con validación clínica estricta con GPT-5.6 Codex.
- **Dependencies**:
- **Files**:
  - `packages/db/migrations/0003_paciente_sexo_not_null.sql`
  - `packages/db/schema.sql`
  - `packages/lib/schemas/paciente.ts`
  - `packages/lib/schemas/paciente.test.ts`

### F6.1.T6 — Pacientes Dominio: utilidades de edad desglosada y etapas pediátricas

**Objetivo**

Implementar en `@labo/lib/edad.ts` la función pura `calcularEdadDesglosada` con cálculo preciso de años, meses y días, clasificación de etapas clínicas (Neonato a Adulto Mayor) y tests unitarios exhaustivos.

**Alcance**

Sí hace:
- Implementar `calcularEdadDesglosada(fechaNacimiento, fechaReferencia?)` en `packages/lib/edad.ts` retornando `{ anos, meses, dias, etapa, textoFormateado }`.
- Soportar etapas clínicas: Neonato (<28d), Lactante menor (1m-11m), Lactante mayor (12m-23m), Preescolar (2a-5a), Escolar (6a-11a), Adolescente (12a-17a), Adulto (18a-59a), Adulto mayor (60a+).
- Manejar casos borde: años bisiestos, diferencias de fin de mes, fechas futuras (retornar error o null).
- Crear suite exhaustiva de tests unitarios en `packages/lib/edad.test.ts`.

**Criterios de aceptación**

- [ ] `calcularEdadDesglosada` calcula con precisión exacta días, meses y años para cualquier fecha histórica.
- [ ] Clasifica correctamente cada etapa clínica de desarrollo pediátrico y adulto.
- [ ] Formatea textos compactos y legibles (ej: "15 días", "4 meses 12 días", "3 años 2 meses", "28 años").
- [ ] Suite de tests en `packages/lib/edad.test.ts` pasa 100% en verde.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 1.0h
- **Razón**: Lógica de cálculo matemático/clínico y suite de tests unitarios puros con DeepSeek V4 Pro.
- **Dependencies**:
- **Files**:
  - `packages/lib/edad.ts`
  - `packages/lib/edad.test.ts`

### F6.1.T7 — Pacientes UI: reactividad de edad en vivo, badge pediátrico y select obligatorio

**Objetivo**

Integrar en `PacienteFormDialog.tsx` el cálculo reactivo de edad en vivo al tipear la fecha de nacimiento, mostrar badge pediátrico dinámico con etapa clínica, hacer obligatorio el select de sexo en la UI y sincronizar listados y tests E2E.

**Alcance**

Sí hace:
- Integrar `useWatch` / `watch("fecha_nacimiento")` en `apps/web/app/(app)/pacientes/PacienteFormDialog.tsx` conectando con `calcularEdadDesglosada`.
- Mostrar badge dinámico con desglose de edad y etapa clínica inmediatamente debajo del input de fecha de nacimiento.
- Configurar selector de sexo como obligatorio sin valor default ambiguo, mostrando feedback visual de error.
- Mostrar indicador de edad desglosada y badge pediátrico en `apps/web/app/(app)/pacientes/PacientesList.tsx`.
- Actualizar suite E2E en `apps/web/e2e/paciente.spec.ts`.

**Criterios de aceptación**

- [ ] Al ingresar o modificar la fecha de nacimiento, el badge de edad y etapa se actualiza en vivo sin demora.
- [ ] El diálogo bloquea el submit si no se selecciona explícitamente sexo 'M' o 'F'.
- [ ] El listado de pacientes muestra la edad formateada adecuadamente para pacientes pediátricos.
- [ ] Tests E2E de pacientes en Playwright pasan al 100% en verde.

- **Modelo**: opencode/gemini-3.1-pro
- **Estimación**: 2.0h
- **Razón**: Formulario reactivo con React Hook Form, feedback visual dinámico y tests de interfaz con Gemini 3.1 Pro.
- **Dependencies**: F6.1.T5, F6.1.T6
- **Files**:
  - `apps/web/app/(app)/pacientes/PacienteFormDialog.tsx`
  - `apps/web/app/(app)/pacientes/PacientesList.tsx`
  - `apps/web/e2e/paciente.spec.ts`

## F6.2 — Package: pipeline

### F6.2.T1 — Presupuestos DDL & Schemas: estados de pipeline y motivo de rechazo

**Objetivo**

Actualizar el esquema PostgreSQL y los contratos Zod para soportar los 6 estados del pipeline comercial (`Borrador`, `Enviado`, `Aprobado`, `Rechazado`, `Cancelado`, `Convertido`), agregar la columna `motivo_rechazo` y tests unitarios.

**Alcance**

Sí hace:
- Crear migración SQL `packages/db/migrations/0004_presupuestos_pipeline_estados.sql` con constraint de estados ampliados y columna `motivo_rechazo text`.
- Actualizar `packages/db/schema.sql` reflejando las nuevas columnas y constraints.
- Actualizar `packages/lib/schemas/presupuesto.ts` con `PresupuestoEstadoEnum` y validación condicional que exija `motivo_rechazo` (mínimo 3 caracteres) cuando el estado sea `Rechazado`.
- Añadir tests unitarios en `packages/lib/schemas/presupuesto.test.ts`.

**Criterios de aceptación**

- [ ] Migración `0004_presupuestos_pipeline_estados.sql` aplica limpia en PostgreSQL.
- [ ] Schema Zod valida transiciones y rechaza estado `Rechazado` si `motivo_rechazo` está vacío.
- [ ] Tests unitarios `pnpm --filter @labo/lib test` pasan en verde.

- **Modelo**: opencode/gpt-5.6-codex
- **Estimación**: 1.0h
- **Razón**: Migración DDL SQL y tipado de esquemas Zod con GPT-5.6 Codex.
- **Dependencies**:
- **Files**:
  - `packages/db/migrations/0004_presupuestos_pipeline_estados.sql`
  - `packages/db/schema.sql`
  - `packages/lib/schemas/presupuesto.ts`
  - `packages/lib/schemas/presupuesto.test.ts`

### F6.2.T2 — Presupuestos Repositorios y API: máquina de estados y endpoint PATCH

**Objetivo**

Implementar en `packages/db/repos/presupuestos.ts` la matriz de transiciones permitidas, filtros por múltiples estados, y exponer el endpoint `PATCH /api/presupuestos/[id]` con auditoría de cambios de estado.

**Alcance**

Sí hace:
- Definir matriz de transiciones válidas en `packages/db/repos/presupuestos.ts` (`Borrador` -> `Enviado`/`Cancelado`, `Enviado` -> `Aprobado`/`Rechazado`/`Cancelado`, etc.).
- Implementar función `cambiarEstado(id, nuevoEstado, motivoRechazo?, userId?)` que valide la transición y registre auditoría.
- Soportar filtrado por array de estados en `presupuestosRepo.list`.
- Crear o actualizar Route Handler `apps/web/app/api/presupuestos/[id]/route.ts` para peticiones `PATCH`.
- Crear tests de integración en `packages/db/repos/presupuestos.integration.test.ts`.

**Criterios de aceptación**

- [ ] Intentos de transiciones no permitidas devuelven error 400 `TRANSICION_ESTADO_INVALIDA`.
- [ ] Al cambiar a `Rechazado`, `motivo_rechazo` se almacena correctamente en la base de datos.
- [ ] Peticiones `PATCH /api/presupuestos/[id]` actualizan el estado y generan entrada en `audit_log`.
- [ ] Tests de integración en `@labo/db` pasan en verde.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 1.5h
- **Razón**: Máquina de estados SQL, repositorios transaccionales y Route Handlers con DeepSeek V4 Pro.
- **Dependencies**: F6.2.T1
- **Files**:
  - `packages/db/repos/presupuestos.ts`
  - `packages/db/repos/presupuestos.integration.test.ts`
  - `apps/web/app/api/presupuestos/[id]/route.ts`

### F6.2.T3 — Presupuestos UI: Pipeline Kanban, toggle de vista y acciones rápidas

**Objetivo**

Construir la interfaz de Pipeline comercial con tablero Kanban por columnas de estado, acumuladores de totales en USD/Bs, selector de visualización [Tabla | Kanban] en `PresupuestosList.tsx` y modal de acciones rápidas de cambio de estado.

**Alcance**

Sí hace:
- Crear componente `packages/ui/presupuestos/PresupuestoPipelineKanban.tsx` con columnas por etapa, totales acumulados y tarjetas con badges de estado.
- Crear componente `packages/ui/presupuestos/PresupuestoEstadoBadge.tsx` con estilos visuales diferenciados por estado.
- Añadir toggle [Tabla | Kanban] en `apps/web/app/(app)/presupuestos/PresupuestosList.tsx` o `page.tsx` permitiendo alternar vistas fluidamente.
- Implementar modal de acciones rápidas para registrar envío, aprobar, rechazar con motivo obligatorio o convertir a resultado.

**Criterios de aceptación**

- [ ] Toggle [Tabla | Kanban] permite alternar entre vista tabular tradicional y tablero Kanban sin recargar la página.
- [ ] Las columnas del Kanban calculan y muestran el conteo de presupuestos y la sumatoria total en USD y Bs.
- [ ] Diálogo de rechazo exige ingresar un motivo antes de confirmar el cambio de estado.
- [ ] Flujo de conversión redirige al alta de resultado con los datos del presupuesto precargados.

- **Modelo**: opencode/gemini-3.1-pro
- **Estimación**: 2.5h
- **Razón**: Construcción de tablero Kanban interactivo, componentes visuales de estado y modales de acción con Gemini 3.1 Pro.
- **Dependencies**: F6.2.T2
- **Files**:
  - `packages/ui/presupuestos/PresupuestoPipelineKanban.tsx`
  - `packages/ui/presupuestos/PresupuestoEstadoBadge.tsx`
  - `apps/web/app/(app)/presupuestos/PresupuestosList.tsx`
  - `apps/web/app/(app)/presupuestos/page.tsx`

## F6.3 — Package: catalogo

### F6.3.T1 — Catálogo DDL & Schemas: columnas tipo_analisis y metodo con snapshots

**Objetivo**

Crear la migración `0005_examenes_tipo_analisis_metodo.sql` para agregar `tipo_analisis` y `metodo` en la tabla `examenes` y sus columnas de snapshot (`tipo_analisis_snap`, `metodo_snap`) en `resultados_examenes`, actualizando `schema.sql` y esquemas Zod con tests.

**Alcance**

Sí hace:
- Crear migración SQL `packages/db/migrations/0005_examenes_tipo_analisis_metodo.sql` con `ALTER TABLE examenes ADD COLUMN tipo_analisis text, ADD COLUMN metodo text;` y `ALTER TABLE resultados_examenes ADD COLUMN tipo_analisis_snap text, ADD COLUMN metodo_snap text;`.
- Actualizar `packages/db/schema.sql`.
- Actualizar `packages/lib/schemas/examen.ts` incorporando validaciones Zod para `tipo_analisis` (opcional) y `metodo` (opcional).
- Añadir tests unitarios en `packages/lib/schemas/examen.test.ts`.

**Criterios de aceptación**

- [ ] Migración `0005_examenes_tipo_analisis_metodo.sql` aplica limpia en PostgreSQL.
- [ ] `packages/db/schema.sql` refleja las nuevas columnas de catálogo y snapshots.
- [ ] Schema Zod valida correctamente campos `tipo_analisis` y `metodo` como strings sanitizados opcionales.
- [ ] Tests unitarios en `@labo/lib` pasan en verde.

- **Modelo**: opencode/gpt-5.6-codex
- **Estimación**: 1.0h
- **Razón**: Migración SQL DDL y esquemas Zod con GPT-5.6 Codex.
- **Dependencies**:
- **Files**:
  - `packages/db/migrations/0005_examenes_tipo_analisis_metodo.sql`
  - `packages/db/schema.sql`
  - `packages/lib/schemas/examen.ts`
  - `packages/lib/schemas/examen.test.ts`

### F6.3.T2 — Catálogo Excel & Repositorios: importación y captura de snapshots

**Objetivo**

Actualizar el parser de importación `packages/lib/xlsx-import.ts` y la plantilla de ejemplo para soportar columnas de "Tipo de Análisis" y "Método", y actualizar métodos en `packages/db/repos/examenes.ts` y `packages/db/repos/resultados.ts` para persistir y capturar snapshots.

**Alcance**

Sí hace:
- Actualizar `packages/lib/xlsx-import.ts` para mapear encabezados de Tipo de Análisis y Método.
- Actualizar `apps/web/public/plantilla-import.xlsx` incorporando columnas de ejemplo.
- Actualizar métodos `create`, `update` y `upsertBatch` en `packages/db/repos/examenes.ts` para guardar `tipo_analisis` y `metodo`.
- Actualizar `packages/db/repos/resultados.ts` para copiar `tipo_analisis` y `metodo` a los campos snapshot al instanciar un resultado.

**Criterios de aceptación**

- [ ] Parser de Excel procesa correctamente archivos XLSX con o sin columnas de Tipo de Análisis y Método.
- [ ] Operaciones de guardado en catálogo persisten los nuevos campos.
- [ ] Creación de resultado clínico almacena snapshots inmutables de tipo y método.
- [ ] Tests unitarios de importación pasan en verde.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 1.5h
- **Razón**: Lógica de parser SheetJS, repositorios SQL y persistencia de snapshots con DeepSeek V4 Pro.
- **Dependencies**: F6.3.T1
- **Files**:
  - `packages/lib/xlsx-import.ts`
  - `apps/web/public/plantilla-import.xlsx`
  - `packages/db/repos/examenes.ts`
  - `packages/db/repos/resultados.ts`

### F6.3.T3 — Catálogo UI: formulario de examen y visualización en navegador de títulos

**Objetivo**

Extender `ExamenFormDialog.tsx` para permitir capturar y editar `tipo_analisis` y `metodo`, y actualizar `TitulosNavigator.tsx` para mostrar badges informativos de tipo de análisis y método en el catálogo.

**Alcance**

Sí hace:
- Agregar campos de formulario para `tipo_analisis` y `metodo` en `apps/web/app/(app)/examenes/ExamenFormDialog.tsx`.
- Mostrar badges y etiquetas descriptivas de tipo de análisis y método en los listados y tarjetas de `apps/web/app/(app)/examenes/TitulosNavigator.tsx`.
- Manejar sugerencias o selects de autocompletado para tipos de análisis comunes si aplica.

**Criterios de aceptación**

- [ ] Modal de examen permite crear y editar Tipo de Análisis y Método.
- [ ] Navegador de títulos y catálogo listan badges visuales claros de método y tipo.
- [ ] Si un examen no define método ni tipo de análisis, la UI mantiene una disposición limpia sin artefactos vacíos.

- **Modelo**: opencode/gemini-3.1-pro
- **Estimación**: 1.5h
- **Razón**: Componentes de interfaz de usuario de catálogo y diálogos con Gemini 3.1 Pro.
- **Dependencies**: F6.3.T1
- **Files**:
  - `apps/web/app/(app)/examenes/ExamenFormDialog.tsx`
  - `apps/web/app/(app)/examenes/TitulosNavigator.tsx`

### F6.3.T4 — Resultados PDF & Tabla: agrupación jerárquica por tipo de análisis y método

**Objetivo**

Actualizar el renderizado del informe clínico en `@labo/pdf/ResultadoPDF.tsx` y la tabla de resultados (`ExamenesTable.tsx`) para agrupar por Grupo (Título), sub-agrupar por Tipo de Análisis y mostrar la línea de Método.

**Alcance**

Sí hace:
- Modificar `packages/pdf/components/ExamenesTable.tsx` y `packages/pdf/ResultadoPDF.tsx` para estructurar la tabla jerárquicamente: Título -> Sub-encabezado de Tipo de Análisis -> Lista de Exámenes.
- Imprimir la leyenda `Método: {metodo_snap}` debajo del nombre de cada examen cuando esté definido.
- Mantener diseño limpio y compacto en `@react-pdf/renderer` asegurando saltos de página adecuados sin cortar grupos.

**Criterios de aceptación**

- [ ] El reporte PDF renderiza sub-encabezados de Tipo de Análisis dentro de cada grupo de examen.
- [ ] Muestra el método clínico correspondiente junto a cada examen que posea `metodo_snap`.
- [ ] Exámenes sin sub-agrupación ni método se renderizan limpiamente con espaciado consistente.

- **Modelo**: opencode/gemini-3.1-pro
- **Estimación**: 2.0h
- **Razón**: Plantillas de reporte médico en @react-pdf y componentes de tabla con Gemini 3.1 Pro.
- **Dependencies**: F6.3.T2, F6.6.T2
- **Files**:
  - `packages/pdf/components/ExamenesTable.tsx`
  - `packages/pdf/ResultadoPDF.tsx`

## F6.4 — Package: paquetes

### F6.4.T1 — Paquetes DDL & Schemas: columna precio_base y repositorios

**Objetivo**

Crear la migración `0006_paquetes_precio_base.sql` para añadir la columna `precio_base numeric(12,2) NOT NULL DEFAULT 0` a la tabla `paquetes`, actualizar esquemas Zod en `@labo/lib/schemas/paquete.ts` y métodos en `packages/db/repos/paquetes.ts`.

**Alcance**

Sí hace:
- Crear migración SQL `packages/db/migrations/0006_paquetes_precio_base.sql` agregando `precio_base numeric(12,2) NOT NULL DEFAULT 0;`.
- Actualizar `packages/db/schema.sql`.
- Actualizar esquemas Zod en `packages/lib/schemas/paquete.ts` validando `precio_base: z.number().min(0)`.
- Actualizar métodos CRUD en `packages/db/repos/paquetes.ts` para persistir y retornar `precio_base`.

**Criterios de aceptación**

- [ ] Migración `0006_paquetes_precio_base.sql` aplica correctamente en PostgreSQL.
- [ ] Schema Zod rechaza precios base negativos.
- [ ] Repositorio de paquetes persiste y recupera `precio_base`.
- [ ] Tests unitarios en `@labo/lib` pasan en verde.

- **Modelo**: opencode/gpt-5.6-codex
- **Estimación**: 1.0h
- **Razón**: Migración SQL DDL, esquemas Zod y repositorio de paquetes con GPT-5.6 Codex.
- **Dependencies**:
- **Files**:
  - `packages/db/migrations/0006_paquetes_precio_base.sql`
  - `packages/db/schema.sql`
  - `packages/lib/schemas/paquete.ts`
  - `packages/db/repos/paquetes.ts`

### F6.4.T2 — Paquetes UI: constructor con selección por grupos y cálculo reactivo de ahorro

**Objetivo**

Actualizar `PaqueteBuilder.tsx` para soportar la selección masiva de exámenes por Grupos completos mediante acordeón, input de `precio_base` y cálculo de porcentaje de ahorro en tiempo real contra la suma individual.

**Alcance**

Sí hace:
- Rediseñar el selector de exámenes en `apps/web/app/(app)/paquetes/PaqueteBuilder.tsx` con un acordeón de grupos/títulos y botón "Agregar grupo completo".
- Permitir la adición o remoción de exámenes individuales dentro de cada grupo.
- Añadir campo de entrada para `precio_base` (USD) con validación numérica.
- Mostrar tarjeta/badge reactivo con el desglose en tiempo real: Suma individual vs Precio base paquete y % de ahorro resultante.

**Criterios de aceptación**

- [ ] Botón "Agregar grupo completo" añade todos los exámenes activos del título seleccionado sin duplicaciones.
- [ ] Al tipear `precio_base`, la interfaz recalcula en vivo el monto y porcentaje de ahorro.
- [ ] Muestra advertencia visual clara si el precio base configurado es superior a la suma individual.
- [ ] Guardar paquete persiste correctamente la lista de exámenes y el `precio_base`.

- **Modelo**: opencode/gemini-3.1-pro
- **Estimación**: 2.0h
- **Razón**: Interfaz de constructor visual con cálculo reactivo y acordeón interactivo con Gemini 3.1 Pro.
- **Dependencies**: F6.4.T1
- **Files**:
  - `apps/web/app/(app)/paquetes/PaqueteBuilder.tsx`
  - `apps/web/app/(app)/paquetes/page.tsx`

## F6.5 — Package: presupuestos-cart

### F6.5.T1 — Presupuestos UI: componente accesible ExamenAutocomplete con auto-foco

**Objetivo**

Implementar el componente accesible `@labo/ui/examenes/ExamenAutocomplete.tsx` con debounce de 250ms, navegación completa por teclado, retención de auto-foco post-selección y badges informativos para ítems ya agregados.

**Alcance**

Sí hace:
- Crear `packages/ui/examenes/ExamenAutocomplete.tsx` implementando el patrón Combobox accesible WAI-ARIA.
- Debounce de 250ms en la búsqueda contra el catálogo.
- Soporte para navegación con teclas `ArrowDown`, `ArrowUp`, `Enter` para seleccionar y `Escape` para cerrar.
- Soporte de prop `selectedIds?: string[]` para deshabilitar y marcar ítems ya agregados.
- Prop `autoFocusOnSelect?: boolean` para devolver el foco automáticamente al input tras seleccionar un examen.
- Crear tests unitarios / testing-library en `packages/ui/examenes/ExamenAutocomplete.test.tsx`.

**Criterios de aceptación**

- [ ] La búsqueda debouncea peticiones a 250ms.
- [ ] El teclado permite navegar por las opciones y seleccionar con tecla Enter sin requerir cursor.
- [ ] Si `autoFocusOnSelect` está activo, el foco se mantiene en el campo de texto tras agregar un ítem.
- [ ] Tests de componente en `@labo/ui` pasan en verde.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 1.5h
- **Razón**: Componente Combobox accesible, debounce y control de foco con DeepSeek V4 Pro.
- **Dependencies**:
- **Files**:
  - `packages/ui/examenes/ExamenAutocomplete.tsx`
  - `packages/ui/examenes/ExamenAutocomplete.test.tsx`

### F6.5.T2 — Presupuestos Engine: migración presupuestos_lineas_pricing y cálculo por línea

**Objetivo**

Crear la migración `0007_presupuestos_lineas_pricing.sql` añadiendo `paquete_id`, `precio_base_snap`, `ganancia_pct` y `precio_final_snap` a `presupuestos_examenes`, actualizar esquemas Zod y adaptar `@labo/lib/calcular-totales.ts` para cálculo aritmético exacto por línea.

**Alcance**

Sí hace:
- Crear migración SQL `packages/db/migrations/0007_presupuestos_lineas_pricing.sql` con las columnas de pricing por línea en `presupuestos_examenes`.
- Actualizar `packages/db/schema.sql`.
- Actualizar `packages/lib/schemas/presupuesto.ts` tipando las líneas con `paquete_id`, `precio_base_snap`, `ganancia_pct`, `precio_final_snap`.
- Actualizar función `calcularTotales` en `packages/lib/calcular-totales.ts` calculando `precio_final = precio_base * (1 + ganancia_pct / 100)` y garantizando que la suma de las líneas coincida al centavo con el total general sin desfases por redondeo.

**Criterios de aceptación**

- [ ] Migración `0007_presupuestos_lineas_pricing.sql` aplica limpia en PostgreSQL.
- [ ] `calcularTotales` satisface exactamente `suma(lineas.precio_final) === total_usd` sin diferencias por centavos.
- [ ] Esquemas Zod validan líneas con porcentaje de ganancia individual o global.
- [ ] Tests unitarios en `packages/lib/calcular-totales.test.ts` pasan al 100% en verde.

- **Modelo**: opencode/gpt-5.6-codex
- **Estimación**: 1.5h
- **Razón**: Migración SQL DDL, schemas Zod y motor de cálculo aritmético con GPT-5.6 Codex.
- **Dependencies**: F6.4.T1
- **Files**:
  - `packages/db/migrations/0007_presupuestos_lineas_pricing.sql`
  - `packages/db/schema.sql`
  - `packages/lib/schemas/presupuesto.ts`
  - `packages/lib/calcular-totales.ts`

### F6.5.T3 — Presupuestos UI: formulario Dual Mode, carrito reactivo y edición granular

**Objetivo**

Actualizar `PresupuestoForm.tsx` para soportar selección de paquetes en Modo A (Cerrado) o Modo B (Desglosado), permitir eliminar exámenes individuales con botón Trash, editar porcentajes de ganancia global o por línea, e integrar `ExamenAutocomplete`.

**Alcance**

Sí hace:
- Integrar `ExamenAutocomplete` para adición ágil de exámenes individuales.
- Implementar selector dual al cargar un paquete: Modo A (Cerrado como ítem único con su precio base) vs Modo B (Desglosado con todos sus exámenes).
- Permitir eliminar cualquier examen del carrito con botón Trash, recalculando totales automáticamente.
- Habilitar ajuste de ganancia global o granular por línea individual.
- Actualizar vista de detalle `apps/web/app/(app)/presupuestos/[id]/PresupuestoDetalle.tsx` para mostrar líneas y paquetes según corresponda.

**Criterios de aceptación**

- [ ] Carga de exámenes individuales es fluida con `ExamenAutocomplete` y foco continuo.
- [ ] Selección de paquete permite elegir entre Modo A (Cerrado) y Modo B (Desglosado).
- [ ] Botón Trash elimina ítems específicos y recalcula de inmediato los totales en USD y Bs.
- [ ] Modificación de porcentaje de ganancia actualiza los precios finales en vivo.

- **Modelo**: opencode/gemini-3.1-pro
- **Estimación**: 2.5h
- **Razón**: Formulario reactivo complejo con selección dual de paquetes, carrito dinámico y controles de ganancia con Gemini 3.1 Pro.
- **Dependencies**: F6.4.T2, F6.5.T1, F6.5.T2
- **Files**:
  - `apps/web/app/(app)/presupuestos/nuevo/PresupuestoForm.tsx`
  - `apps/web/app/(app)/presupuestos/[id]/PresupuestoDetalle.tsx`

### F6.5.T4 — Presupuestos PDF & Conversión: transparencia de precios y orden clínica

**Objetivo**

Actualizar `@labo/pdf/PresupuestoPDF.tsx` para renderizar precios finales transparentes por línea coincidiendo exactamente con el total general, y asegurar que la conversión de presupuesto a resultado clínico (`convertirPresupuestoAResultado`) instancie únicamente los exámenes conservados.

**Alcance**

Sí hace:
- Modificar `packages/pdf/PresupuestoPDF.tsx` para imprimir cada línea con su `precio_final_snap` asegurando que la suma visual de líneas coincida al centavo con el total en USD y Bs.
- Eliminar conceptos ambiguos o recargos no transparentes en el documento impreso.
- Actualizar `packages/db/repos/presupuestos.ts` y el endpoint `/api/presupuestos/[id]/convertir` para transferir a la orden de resultados únicamente los exámenes activos presentes en el presupuesto.

**Criterios de aceptación**

- [ ] El PDF del presupuesto presenta líneas con precios transparentes cuya sumatoria cuadra exactamente con el total general.
- [ ] La conversión de presupuesto genera una orden de resultados que incluye exclusivamente los exámenes conservados en el presupuesto.
- [ ] No se transfieren a la orden clínica exámenes que hayan sido eliminados del presupuesto previo.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 1.5h
- **Razón**: Lógica de conversión transaccional en backend y formato de documento comercial PDF con DeepSeek V4 Pro.
- **Dependencies**: F6.5.T3, F6.6.T2
- **Files**:
  - `packages/pdf/PresupuestoPDF.tsx`
  - `packages/db/repos/presupuestos.ts`
  - `apps/web/app/api/presupuestos/[id]/convertir/route.ts`

## F6.6 — Package: branding-pdf

### F6.6.T1 — Schemas & Config Institucional: RIF flexible, campos bioanálisis y seed

**Objetivo**

Flexibilizar la validación del RIF en `@labo/lib/schemas/config.ts` para aceptar personas naturales y jurídicas venezolanas (`^[VJGPEC]-\d{7,9}-\d$`), enriquecer la entidad institucional con `colegio_bioanalistas` y `mpps`, y actualizar la migración/seed de `laboratorio_config` con los datos profesionales de Lcda. Yuna Ramírez.

**Alcance**

Sí hace:
- Actualizar `RIF_REGEX` en `packages/lib/schemas/config.ts` al patrón flexible `/^[VJGPEC]-\d{7,9}-\d$/`.
- Enriquecer `configUpdateSchema` y `configUpdatePartialSchema` con los campos opcionales `colegio_bioanalistas` y `mpps`.
- Crear migración SQL `packages/db/migrations/0008_laboratorio_config_institucional.sql` para añadir `colegio_bioanalistas text` y `mpps text` a la tabla `laboratorio_config`.
- Actualizar `packages/db/schema.sql` y `packages/db/repos/config.ts` para persistir, leer y auditar los nuevos campos.
- Actualizar el seed de configuración inicial con los datos oficiales: Lcda. Yuna Ramírez, Colegio de Bioanalistas N° 713, MPPS 10738, RIF institucional, teléfono y correo electrónico.
- Crear tests unitarios en `packages/lib/schemas/config.test.ts` y tests de repo en `packages/db/repos/config.integration.test.ts`.

**Criterios de aceptación**

- [ ] `RIF_REGEX` valida correctamente RIFs de personas naturales y jurídicas (`V-14794920-8`, `J-12345678-9`, `G-20000123-4`, etc.).
- [ ] La migración `0008_laboratorio_config_institucional.sql` aplica limpia sobre PostgreSQL.
- [ ] `laboratorio_config` persiste y retorna `colegio_bioanalistas` y `mpps`.
- [ ] Suite de tests unitarios y de integración de configuración pasa 100% en verde.

- **Modelo**: opencode/gpt-5.6-codex
- **Estimación**: 1.5h
- **Razón**: Refactor de esquemas Zod, migración SQL DDL y datos institucionales con GPT-5.6 Codex.
- **Dependencies**:
- **Files**:
  - `packages/lib/schemas/config.ts`
  - `packages/lib/schemas/config.test.ts`
  - `packages/db/migrations/0008_laboratorio_config_institucional.sql`
  - `packages/db/schema.sql`
  - `packages/db/repos/config.ts`

### F6.6.T2 — Plantillas @react-pdf/renderer de RV Laboratorio

**Objetivo**

Rediseñar los componentes y documentos de `@react-pdf/renderer` (`PDFHeader.tsx`, `PacienteInfo.tsx`, `ExamenesTable.tsx`, `PDFFooter.tsx`, `ResultadoPDF.tsx` y `PresupuestoPDF.tsx`) con la identidad visual corporativa de RV Laboratorio: paleta Teal `#0E9090`, bandas de contraste `#DCDCDC` / `#E6E6E6`, columnas `PRUEBA | RESULTADO | UNIDAD | VALOR NORMAL | METODO`, cláusula médica legal y nota de validez de 24h.

**Alcance**

Sí hace:
- Rediseñar `packages/pdf/components/PDFHeader.tsx` integrando el logo de RV Laboratorio, datos de cabecera institucionales (RIF, Colegio de Bioanalistas, MPPS, teléfonos y dirección) y franja de acento Teal `#0E9090`.
- Rediseñar `packages/pdf/components/PacienteInfo.tsx` con disposición estructurada en bandas `#DCDCDC`/`#E6E6E6`, edad desglosada con etapa clínica, sexo, cédula y fecha de toma de muestra.
- Rediseñar `packages/pdf/components/ExamenesTable.tsx` estructurado con cabecera en `#0E9090`, texto blanco en negrita, filas alternadas y las 5 columnas: `PRUEBA`, `RESULTADO`, `UNIDAD`, `VALOR NORMAL`, `METODO`.
- Rediseñar `packages/pdf/components/PDFFooter.tsx` con áreas designadas para firma digitalizada, sello del laboratorio, pie de página personalizable y cláusula médica legal de validez clínica y confidencialidad.
- Actualizar `packages/pdf/ResultadoPDF.tsx` para agrupar jerárquicamente por Título y Tipo de Análisis, mostrando el método clínico en su columna correspondiente.
- Actualizar `packages/pdf/PresupuestoPDF.tsx` con el nuevo diseño Teal `#0E9090`, desglose transparente de líneas en USD y Bs, y nota legal destacada: "Presupuesto válido por 24 horas a partir de su emisión".
- Crear o actualizar tests de renderizado en `packages/pdf`.

**Criterios de aceptación**

- [ ] Todas las plantillas PDF utilizan Teal `#0E9090` como color primario institucional.
- [ ] La tabla de exámenes renderiza exactamente las columnas `PRUEBA | RESULTADO | UNIDAD | VALOR NORMAL | METODO`.
- [ ] El pie de página incluye la cláusula médica legal de bioanálisis y validez clínica.
- [ ] `PresupuestoPDF.tsx` incluye la cláusula explícita de validez por 24 horas y montos transparentes en USD/Bs.
- [ ] Renderizado sin desbordes de página ni solapamiento de elementos en `@react-pdf/renderer`.

- **Modelo**: opencode/gemini-3.1-pro
- **Estimación**: 2.5h
- **Razón**: Rediseño visual corporativo Teal #0E9090, componentes estructurados y plantillas @react-pdf con Gemini 3.1 Pro.
- **Dependencies**: F6.6.T1
- **Files**:
  - `packages/pdf/components/PDFHeader.tsx`
  - `packages/pdf/components/PacienteInfo.tsx`
  - `packages/pdf/components/ExamenesTable.tsx`
  - `packages/pdf/components/PDFFooter.tsx`
  - `packages/pdf/ResultadoPDF.tsx`
  - `packages/pdf/PresupuestoPDF.tsx`

### F6.6.T3 — Assets & Storage Seed (Logo, Firma, Sello y Endpoint Resolution)

**Objetivo**

Procesar y optimizar los assets visuales corporativos (`1.png`, firma y sello de `docs/logoypdfsrvlaboratorio/`), construir script de seed / carga al bucket `assets` de InsForge Storage y verificar la resolución de URLs en los endpoints de generación de PDF.

**Alcance**

Sí hace:
- Procesar y optimizar `docs/logoypdfsrvlaboratorio/1.png` para generar el logo oficial liviano (< 150KB) en formato PNG transparente.
- Extraer y vectorizar/optimizar los assets de firma y sello de la bioanalista responsable desde los documentos de muestra en `docs/logoypdfsrvlaboratorio/`.
- Crear script de inicialización `packages/db/scripts/seed-assets.ts` para sincronizar los assets al bucket `assets` de InsForge Storage y registrar sus `object_key` en `laboratorio_config`.
- Disponer copias de fallback en `apps/web/public/assets/` para generación local y testing offline.
- Verificar que los Route Handlers `apps/web/app/api/pdf/resultado/[id]/route.ts` y `apps/web/app/api/pdf/presupuesto/[id]/route.ts` resuelvan correctamente las URLs públicas firmadas de los assets sin bloqueos de red.
- Tests de integración de resolución de assets para exportación PDF.

**Criterios de aceptación**

- [ ] Logo, firma y sello optimizados están disponibles con fondo transparente y peso liviano (< 200KB).
- [ ] El script de seed de assets ejecuta idempotentemente y actualiza `laboratorio_config`.
- [ ] Los endpoints `/api/pdf/resultado/[id]` y `/api/pdf/presupuesto/[id]` descargan y embeben los assets en el PDF generado sin errores.
- [ ] Tests de integración de endpoints PDF pasan en verde.

- **Modelo**: opencode/deepseek-v4-pro
- **Estimación**: 1.5h
- **Razón**: Optimización de imágenes, scripts de seed en InsForge Storage y endpoints PDF con DeepSeek V4 Pro.
- **Dependencies**: F6.6.T1
- **Files**:
  - `packages/db/scripts/seed-assets.ts`
  - `apps/web/app/api/pdf/resultado/[id]/route.ts`
  - `apps/web/app/api/pdf/presupuesto/[id]/route.ts`
  - `apps/web/public/assets/logo.png`
  - `apps/web/public/assets/firma.png`
  - `apps/web/public/assets/sello.png`
