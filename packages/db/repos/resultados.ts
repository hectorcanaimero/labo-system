/**
 * @deprecated El módulo fue renombrado. La entidad se llama ahora "orden"
 * (orden de servicio del laboratorio). Este archivo re-exporta desde
 * `./ordenes` para mantener compatibilidad con imports viejos.
 * Los nombres legacy (`Resultado`, `RESULTADO_NO_ENCONTRADO`, etc.) siguen
 * disponibles a través de aliases dentro de `./ordenes`.
 */
export * from "./ordenes";
