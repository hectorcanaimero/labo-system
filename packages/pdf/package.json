{
  "name": "@labo/pdf",
  "version": "0.0.0",
  "private": true,
  "dependencies": {
    "convex": "^1.45.0"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "eslint . --ext .ts,.tsx --no-error-on-unmatched-pattern",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "eslint": "^8.57.0"
  }
}
