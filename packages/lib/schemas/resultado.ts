/**
 * @deprecated Este módulo fue renombrado. La entidad se llama ahora "orden"
 * (orden de servicio del laboratorio). Se mantiene este archivo como
 * re-export para no romper imports existentes — nuevos usos deberían
 * importar desde `@labo/lib/schemas/orden`.
 */
export * from "./orden";
