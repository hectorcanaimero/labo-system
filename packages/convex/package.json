{
  "name": "@labo/convex",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'paquete legacy post-pivot a InsForge (ADR-11), no se despliega, no-op' && exit 0",
    "dev": "convex dev",
    "lint": "eslint . --ext .ts,.tsx --no-error-on-unmatched-pattern",
    "typecheck": "tsc --noEmit",
    "test": "echo 'paquete legacy post-pivot a InsForge (ADR-11), sin vitest instalado, no-op' && exit 0"
  },
  "dependencies": {
    "@convex-dev/auth": "^0.0.95",
    "@labo/lib": "workspace:*",
    "convex": "^1.45.0",
    "convex-helpers": "^0.1.123"
  },
  "devDependencies": {
    "@types/node": "^22.20.1",
    "eslint": "^8.57.0"
  }
}
